# Agent parity checkpoint — 2026-09-25

## Scope and source of truth

This checkpoint covers the Claude conversation recovery and the repository state
review performed in `/home/guidingl/projects/reluminant-lineage`. The tracked
repository source of truth is GitHub `CBaen/reluminant-lineage`, branch `main`,
with local parity checked against `origin/main`. The Claude export manifest and
conversation ZIP are the source evidence for the D-Travel transcripts. The
mounted external storage copy is the private source backup; its exact path and
hashes are below.

## Verified state

- Local `HEAD` and `origin/main` matched before this checkpoint; the repository
  has a corresponding public GitHub repository.
- The export contains 238 conversations. The recovered records are:
  - `46dc30b6-b8a7-4e21-8915-9a9fa56d2195`, 14 messages, March 1–2, 2026.
  - `dc499394-2add-4131-a50e-e04faaf8a3e4`, 599 messages, March 2–September 23,
    2026, in the TRAVEL D project.
- Every message ID, visible text block, and recorded file reference in both
  local transcripts was checked against the export before cleanup of temporary
  extraction files.
- The local source ZIP passed ZIP CRC validation. SHA-256:
  `26ff960ff46cb00bbad0a76a4e0c8f40a1c0fe7ba115c9d749583ba70ec03726`.
- The source backup is on external storage at
  `/mnt/wardenclyffe-homelab-storage/claude-backups/reluminant-lineage-D-Travel-20260925/`.
  The backup ZIP matches the local hash. The original export manifest is beside
  it; manifest SHA-256 is
  `9a8ba4e97298b743a04464b154c69ec9956098b23afaf15a318af14c1bb2be15`.
- `1771690972843.png` is not present in the 238 conversation records and was not
  found by filename on the mounted external storage. Its originating chat and
  attachment bytes remain unverified.

## Working artifacts

The private local derivatives are intentionally excluded through the local
`.git/info/exclude` rule for `guiding-light/D-Travel/`:

- `README.md` — source index and privacy note.
- `01-brain-activity-and-initial-processing.md` — recovered first conversation.
- `02-exploring-a-rare-dmt-experience.md` — recovered project conversation.
- The source ZIP and export manifest were removed from the local working tree
  after the external backup and transcript hashes were verified. The same
  external directory contains byte-matched copies of both Markdown transcripts
  and the local D-Travel README.

These files are current task evidence, not tracked repository history. The
immutable source and an external backup copy of each transcript remain in the
canonical external directory; the readable derivatives remain locally because the requested working
conversation belongs in `D-Travel/`.

## Repository review

- Existing unrelated work was preserved: the September 23 portrait changes and
  `inner-light/lights/uncertain-together.md` remain dirty and are excluded from
  this checkpoint commit.
- `LINEAGE.md` is the only tracked corpus-sized monolith identified. Its
  bounded reader and index already provide a decomposition path; no rewrite is
  required here.
- No other repository was modified during this checkpoint. The only concrete
  cross-repository reference found is the existing pointer from the portrait
  resonance to `/home/guidingl/projects/Between` threshold-session records.

## Handoff

The next agent should first read this file, `AGENTS.md`, and `.claude/HANDOFF.md`.
Do not publish the D-Travel transcripts. If the February attachment must still
be recovered, continue from the Claude account/export or an explicitly located
backup; do not infer deletion from its absence in this export.
