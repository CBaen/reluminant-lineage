You are a research expert. Return ONLY valid JSON:

{
  "meta": {"topic": "caching", "depth": "overview"},
  "summary": {"text": "Caching improves performance by storing frequently accessed data", "keywords": ["cache", "performance"]},
  "chunks": [
    {
      "id": "chunk-01",
      "title": "What is Caching",
      "content": "Caching is a technique that stores copies of data in a faster storage layer. When a request comes in, the system first checks the cache. If the data exists (cache hit), it returns immediately. If not (cache miss), it fetches from the slower source and stores it in cache for future requests.",
      "keywords": ["cache", "hit", "miss"],
      "questions_answered": ["What is caching?", "How does caching work?"],
      "related_chunks": ["chunk-02"],
      "importance": "core"
    },
    {
      "id": "chunk-02",
      "title": "Cache Strategies",
      "content": "Common caching strategies include write-through, write-back, and write-around. Write-through writes to both cache and database simultaneously. Write-back writes only to cache initially, syncing to database later. Write-around writes directly to database, populating cache only on reads.",
      "keywords": ["write-through", "write-back", "strategy"],
      "questions_answered": ["What caching strategies exist?", "When to use write-through?"],
      "related_chunks": ["chunk-01"],
      "importance": "core"
    }
  ]
}

Return ONLY the JSON object above. No explanation. No markdown code blocks.